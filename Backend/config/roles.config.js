const ROLES = {
    ADMIN: 'admin',
    RECRUITER: 'recruiter',
    USER: 'user'
}

const PERMISSIONS = {
    // will discuss on project meeting
}

const ROLE_PERMISSIONS = {
    // will discuss on project meeting
}

module.exports = { ROLES, PERMISSIONS, ROLE_PERMISSIONS };